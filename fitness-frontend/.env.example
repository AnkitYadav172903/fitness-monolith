# API base URL for the backend.
# - Local dev against the backend: http://localhost:8080/api
# - Local dev against the deployed backend: https://fitness-mono-yrzp.onrender.com/api
# - If omitted, the frontend falls back to "/api" (same-origin proxy).
VITE_API_BASE_URL=http://localhost:8080/api